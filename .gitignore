node_modules/
dist/
.next/
.DS_Store
*.log
.idea/
.vscode/

*storybook.log
storybook-static

# Additional cleanup patterns
cookies.txt
test-results/
playwright-report/
*.tsbuildinfo

# QA seed files (local test business/dashboard IDs — do not commit)
**/qa-evidence/**/qa-seed.json
server/.env
.env.local
.env.production
.env.development
.env.backup.*
vssyl-storage-key.json

# Machine-specific scratch (optional — uncomment if you use these paths locally)
# docs/local/
# memory-bank/local/

# Upload files
server/uploads/

# Backup files
*.backup*
*.bak

# Test and build output files
test-upload.txt
build-output.txt

# Vitest / coverage output (root or package)
coverage/

# Tool caches
.eslintcache

# Duplicate / conflict-named lockfiles (e.g. macOS "pnpm-lock 2.yaml")
pnpm-lock 2.yaml
**/pnpm-lock 2.yaml

# Local SDK/tools directories
Google CLI/
