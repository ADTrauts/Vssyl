'use client';

import PlaceConsumerExperience from '../../components/place/PlaceConsumerExperience';

/**
 * Standalone `/place` route — PlaceProvider is supplied by `app/place/layout.tsx`.
 */
export default function PlacePage() {
  return <PlaceConsumerExperience />;
}
