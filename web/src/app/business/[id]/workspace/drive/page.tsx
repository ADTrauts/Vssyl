/**
 * Segment URL `/workspace/drive` is rendered by `BusinessWorkspaceContent` (switch mount).
 * This page is not mounted when `shouldRenderWorkspaceChildren` is false.
 */
export default function BusinessWorkspaceDrivePage() {
  return null;
}
