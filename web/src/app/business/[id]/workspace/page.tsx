'use client';

/**
 * Hub route defers to DashboardLayoutWrapper → BusinessWorkspaceContent.
 * Dashboard bootstrap and module mounting are owned by the layout wrapper (Wave 1B).
 */
export default function BusinessWorkspacePage() {
  return null;
}
