/**
 * Segment URL `/workspace/place` is rendered by `BusinessWorkspaceContent` (switch mount).
 * This page is not mounted when `shouldRenderWorkspaceChildren` is false.
 * ENG-1 / RWS-F1 — null deferral satisfies App Router route existence.
 */
export default function BusinessWorkspacePlacePage() {
  return null;
}
