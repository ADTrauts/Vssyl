/**
 * Segment URL `/workspace/chat` is rendered by `BusinessWorkspaceContent` (switch mount).
 * This page is not mounted when `shouldRenderWorkspaceChildren` is false.
 */
export default function BusinessWorkspaceChatPage() {
  return null;
}
