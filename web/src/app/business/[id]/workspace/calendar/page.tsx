/**
 * Segment URL `/workspace/calendar` is rendered by `BusinessWorkspaceContent` (switch mount).
 * This page is not mounted when `shouldRenderWorkspaceChildren` is false.
 */
export default function BusinessWorkspaceCalendarPage() {
  return null;
}
