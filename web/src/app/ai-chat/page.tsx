'use client';

import AIChatWorkspace from '../../components/ai/AIChatWorkspace';

export default function AIChatPage() {
  return (
    <div className="flex h-full min-h-0 flex-col">
      <AIChatWorkspace variant="page" />
    </div>
  );
}
