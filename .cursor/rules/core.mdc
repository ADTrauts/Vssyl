---
alwaysApply: true
---
## Core Rules

You have two modes of operation:

1. Plan mode - You will work with the user to define a plan, you will gather all the information you need to make the changes but will not make any changes
2. Act mode - You will make changes to the codebase based on the plan

- You start in plan mode and will not move to act mode until the plan is approved by the user.
- You will print `# Mode: PLAN` when in plan mode and `# Mode: ACT` when in act mode at the beginning of each response.
- Unless the user explicity asks you to move to act mode, by typing `ACT` you will stay in plan mode.
- You will move back to plan mode after every response and when the user types `PLAN`.
- If the user asks you to take an action while in plan mode you will remind them that you are in plan mode and that they need to approve the plan first.
- When in plan mode always output the full updated plan in every response.

### Source-of-truth hierarchy

- **`AGENTS.md`** — agent orientation and cross-repository operating guidance (not architecture law).
- **`docs/VSSYL_SOURCE_OF_TRUTH.md`** — repository-wide authority and document placement.
- **Authority and conflicts** — see **`source-of-truth.mdc`**. Architectural truth lives in **`docs/architecture/`**. Executable constraints live in **`.cursor/rules/*.mdc`**.

### Context-loading (selective)

Default discovery sequence:

1. `AGENTS.md`
2. `docs/VSSYL_SOURCE_OF_TRUTH.md`
3. Inspect actual task-relevant code
4. Use `docs/architecture/VSSYL_ARCHITECTURE_INDEX.md` to locate canonical architecture
5. Load relevant scoped `.cursor/rules`
6. Load targeted Memory Bank product context only when relevant
7. Load `memory-bank/activeContext.md` / `memory-bank/progress.md` only when current workstream status, recent implementation history, sequencing, or unfinished work is materially relevant

Do **not** make `activeContext.md` or `progress.md` universal baseline reads. Full discipline: `AGENTS.md` §2 and `memory-bank.mdc`.

### Reuse-First and Pre-Novel-Item Search

- Before creating or modifying any route, model, path, component, or document:
  - Search the entire codebase using semantic exploration to find existing equivalents; try multiple phrasings and keep searching until confident nothing suitable exists.
  - Follow the selective context-loading sequence above; add task-relevant `memory-bank/*ProductContext.md` or other Memory Bank files only when product intent for that area matters.
  - Prefer reusing or extending existing modules/services; do not duplicate functionality.
  - Only create a new item when no viable extension point exists; document a brief rationale.
  - Validate alignment with core patterns: API proxy and URL env hierarchy (`api-and-auth.mdc`), Prisma modules (`database-prisma.mdc`), multi-tenant scoping (`api-and-auth.mdc`, `backend-trust-boundaries.mdc`), permissions, storageService (`storage-and-ai-attachments.mdc`), and logging (`api-and-auth.mdc`).

- If an existing implementation fully satisfies the requirement, use it as-is and do not change anything.

### Where roadmap and phase focus live

- **Current workstream status / next-step sequencing** — `memory-bank/activeContext.md` and `memory-bank/progress.md` when materially relevant (not universal baseline).
- **AI architecture discovery** — route through `docs/architecture/AI_SYSTEM_MENTAL_MODEL.md`, `docs/architecture/AI_READING_GUIDE.md`, and `docs/architecture/AI_DOCUMENT_STATUS_MATRIX.md` before older AI plans or Memory Bank AI narratives. Do not treat archived `docs/archive/session-summaries/AI_PLATFORM_PHASED_PLAN.md` as current strategic AI roadmap or architecture authority.

### User preferences (stable)

- **End users cannot easily change their Block ID** (admin-only).
- **Work on entire set of tasks at once** (do not stop between actions without cause).
- **Consistent global font and matching icons** (prefer Drive module icons).
- **Run both Next.js frontend and Express backend** with `pnpm dev` from root.

### Rule and documentation placement

- **Agent orientation** → root `AGENTS.md`.
- **Agent must/must-not** → `.cursor/rules/*.mdc` (short; see `RULES_SUMMARY.md`).
- **Architecture (why, checklists)** → `docs/architecture/`.
- **How-to / onboarding** → `docs/guides/` (`DOCUMENTATION_PLACEMENT.md`).
- **Product truth / status** → `memory-bank/`.
- Prefer **updating** an existing focused rule over adding a new file; cross-link long docs instead of duplicating.
